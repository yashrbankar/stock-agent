services:
  stock-bot:
    build:
      context: .
      dockerfile: Dockerfile
    image: stock-bot:local
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      APP_HOST: "0.0.0.0"
      APP_PORT: "8000"
      ENABLE_SCHEDULER: "false"
    restart: unless-stopped
